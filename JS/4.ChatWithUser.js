const isFriend = true;
  const isOnline = true;
  const isAvailable = true;
  if (isFriend && isOnline && isAvailable) {
    console.log("Можно открыть чат с пользователем.");
  } else {
    console.log("Нельзя открыть чат с пользователем.");
  }